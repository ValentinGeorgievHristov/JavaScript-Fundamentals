function solve(arr) {            
    arr.forEach((el, i, ar) => console.log(el));


    }
    solve([2, 1, 1, 2, 3, 3, 2, 2, 2, 1])
    //el = element
    //index = индекса до който е достигнал цикъла
    //ar = самия масив