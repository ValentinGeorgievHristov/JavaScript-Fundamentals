function solve(str) {
    //let pass = ""
    if (str.length >= 6 && str.length<=10 && content.matches "[a-zA-Z0-9]*" && str.length-1 ==Number&&str.length ==Number) {
console.log('ok');

    }


}
solve('logIn')