function solve(name, pop, area) {

     let result = (`Town ${name} has population of ${pop} and area ${area} square km.`);
     console.log(result)
}

solve('Sofia',
     1286383,
     492
); // funkciq vav funkciq