Декларирането на функции бива два вида - FUNCTION DECLARATION & FUNCTION EXPRESSION - ВАЖНО !!!
FUNCTION DECLARATION - могат да бъдат извиквни преди декларирането им.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
arguments // (arguments[0]) - специален обект в JS. Никога не трябва да се опитваме да променяме и манипулираме.
Можем да го използваме след като му направим копие. Arguments - е достъпен само с function declaration.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Не трябва да има повтаряне и съвпадения на имена, на променливи и функции.
Function Declaration - може да се използва навсякъде.
Function Expression - може да се използва само след като бъде декларирана. Използвай Function Declaration. 
  Използва се, когато искаме да ограничим изпълнението на кода от създаването на функцията нататък.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Когато видим създадена променлива с _ в началото, този който я е създал подсказва да не се променя защото е ключова.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
INVOKED - извикване на функцията.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Контекста на една функция зависи само от начина, по който е извикана тя.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Павел Колев - най- добрия лектор на JS.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
console.log(arguments) - показва всички компоненти на функцията независимо дали са могли да се отпечатат или не.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
Параметър е променлива, която чака да приеме конкретен аргумент:     Name- Параметър; Maria- Аргумент;
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
arguments
arguments[0] - чрез него достигаме определени индекси на входните данни на функцията. arguments[0] - достъпва първия елемент.
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
function run(a, b, c){          //ако няма зададена стойност b ще има 10 на негово място.
      b = b || 10;
}
run(5);
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
function getGames() {                                                                           //докато функцията се извиква с "mamma" излиза една игра, с всеки друг
    let games = ["Monopoly", "Game of Thrones", "Scrabble"];             юзър показва резултат всички игри.

    function getMyGames(user) {             // function
        if (user === "mamma") {
            return games[0]
        }
        return games;
    };
    return getMyGames

}
let p = getGames();
console.log(p("mamma1"));
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

function grade(num){
    if(num<2){

    }else if(num>=2&&num<=2.99){
    return "Fail";
}else if(num<=3.49){
    return "Poor";
}else if(num<=4.49){
    return "Good"
}else if(num<=5.49){
    return "Very good";
}else if(num<=6.00){
    return "Excellent";
}else{
    return
}
}
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
    function run(a, b, c){             //използваме run на много места като отделно парче код.
        return a + b + c;
 }

 let pesho = run(5, 2, 3);         //  expected outout - 10
 console.log(pesho);    
 
 let gosho = run(1, 2, 3);      //  expected outout - 6
 console.log(gosho);

 let zoro = run(1, 1, 1);     //  expected outout - 3
 console.log(zoro);



ако еместо "return a + b + c;"  има "let d = a + b + c;"    - ще върне "undefunied"
ако има само "retutn" - отново ще върне "undefunied".
 ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
function calculator(x, y, operator) {                                    ВЛОЖЕНИ ФУНКЦИИ!!! ЯКО!!! ЯКО!!! ЯКО !!!

    let distraction = (a, b) => {
        return a - b;
    };

    let sum = (a, b) => {
        return a + b;
    }
    if(operator === '-'){
        return distraction(x, y);
    }else if(operator === '+'){
        return sum(x, y);
    }
}

let b = calculator(1, 2, '+');
console.log(b)
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
function calculate(a ,b ,op) {                                               // отново калкулатора. друг запис. Отново вложени функции!
let calc = function(a, b, op){return op(a, b); };

let add = function(a, b){return a + b };
let substract = function(a, b){return a - b };
let multiply = function(a, b){return a * b };
let divide = function(a, b){return a / b };

switch(op){
    case '+': return calc(a,b, add);
    case '-': return calc(a,b, substract);
    case '*': return calc(a,b, multiply);
    case '/': return calc(a,b, divide);
    }
}
console.log(calculate(2, 4, '+'));   //expected output 6
------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------